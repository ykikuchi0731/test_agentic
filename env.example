# ============================================================================
# ServiceNow Configuration (REQUIRED for pre-processing)
# ============================================================================
# Your ServiceNow instance URL (without https://)
SERVICENOW_INSTANCE=your-instance.service-now.com

# ServiceNow authentication credentials
SERVICENOW_USERNAME=your-username
SERVICENOW_PASSWORD=your-password

# ============================================================================
# Notion API Configuration (REQUIRED for post-processing)
# ============================================================================
# Your Notion integration API key
# Get it from: https://www.notion.so/my-integrations
NOTION_API_KEY=

# Optional: Database ID for organizing imported content
# Get it from the database URL: https://notion.so/{workspace}/{DATABASE_ID}?v=...
NOTION_DATABASE_ID=

# ============================================================================
# API Settings
# ============================================================================
# Timeout for API requests in seconds
API_TIMEOUT=30

# Maximum number of retry attempts for failed requests
MAX_RETRIES=3

# ============================================================================
# File Download Settings
# ============================================================================
# Directory for downloaded files (attachments, etc.)
DOWNLOAD_DIR=./downloads

# Maximum file size to download in bytes (100MB default)
MAX_FILE_SIZE=104857600

# ============================================================================
# Export Settings
# ============================================================================
# Output directory for migration exports (ZIP files, CSV reports, etc.)
MIGRATION_OUTPUT_DIR=./migration_output

# Whether to create ZIP exports for Notion import
CREATE_ZIP_EXPORTS=true

# ============================================================================
# Browser Automation Settings (for Google Docs export - RECOMMENDED)
# ============================================================================
# Directory for exported Google Docs (DOCX files)
BROWSER_DOWNLOAD_DIR=./google_docs_exports

# Run browser in headless mode (no GUI)
# Set to 'true' for production, 'false' for debugging
BROWSER_HEADLESS=false

# Browser type to use: chrome, firefox, or edge
BROWSER_TYPE=chrome

# Timeout for browser operations in seconds
BROWSER_TIMEOUT=30

# ============================================================================
# Google Account Credentials (OPTIONAL - for automated login)
# ============================================================================
# WARNING: Storing passwords is not recommended for security
# Manual login is preferred - leave these empty for manual authentication

# Google account email (optional)
GOOGLE_EMAIL=

# Google account password (optional, use with extreme caution)
GOOGLE_PASSWORD=

# ============================================================================
# Google Workspace API Settings (DEPRECATED - API method disabled)
# ============================================================================
# The following settings are for the Google Drive API method
# This method is currently disabled - use browser automation instead
# Keep these for reference if API access becomes available

# Path to Google OAuth2 credentials JSON file
GOOGLE_CREDENTIALS_FILE=./credentials.json

# Path where OAuth2 token will be saved
GOOGLE_TOKEN_FILE=./token.json

# Output directory for Google Docs exports (API method)
GOOGLE_DOCS_OUTPUT_DIR=./google_docs_exports
